# Internal planning files (audit notes, task lists, handovers, maintenance logs)
PLAN.txt
TODO.md
_after-care/

# Private / Archive
_archive/
_private/

# Python
__pycache__/
*.pyc
*.py[cod]
*.egg-info/
dist/
build/
*.db
*.sqlite
*.sqlite3
data/

# Virtual Environment
.venv/
venv/

# Environment Variables
.env
.env.*
!.env.example
!.env.sample

# Local secrets / credentials
.npmrc
.pypirc
credentials.json
secrets.json
token.json
tokens.json
*.token
*.secret
*recovery*code*
*recovery*codes*

# Private keys / certificates
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer

# IDE
.vscode/
.idea/

# Logs
*.log

# OS
.DS_Store
Thumbs.db
desktop.ini

# Multi-Host, Locks & Cloud-Sync Hygiene
LOCK
LOCK.*
*.lock
LOCK*.txt
LOCK.permissions.json
uv.lock
!package-lock.json
*-conflict-*
*.sync-conflict-*
*.conflict
*-CONFLIT-*
*.sync-temp-*
* (kopie)*
* (copy)*
* (Kopie)*
* (Copy)*
*conflicted copy*
*-ASUS*
*-ASUS-GEI*
*-LAPTOP*
*-WORKSTATION*
*-WORKSTATION-LG*
*-Mac Studio*
*-MacBook*
*.orig
*.rej
*.tmp
*.bak

# SQLite sidecar files (the runtime database lives in ~/.usmc, never in the repo)
*.db-wal
*.db-shm
*.sqlite-wal
*.sqlite-shm

# Local Runtime State & Test Caches
local-state/
state/
.turbo/
.hypothesis/
.nyc_output/
node_modules/
