# Internal planning files (audit notes, task lists, handovers)
PLAN.txt
TODO.md

# Private / Archive
_archive/
_private/

# Python
__pycache__/
*.pyc
*.py[cod]
*.egg-info/
dist/
build/
*.db
*.sqlite
*.sqlite3
data/

# Virtual Environment
.venv/
venv/

# Environment Variables
.env
.env.*
!.env.example
!.env.sample

# Local secrets / credentials
.npmrc
.pypirc
credentials.json
secrets.json
token.json
tokens.json
*.token
*.secret
*recovery*code*
*recovery*codes*

# Private keys / certificates
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer

# IDE
.vscode/
.idea/

# Logs
*.log

# OS
.DS_Store
Thumbs.db
desktop.ini

# Cloud-sync conflict copies (OneDrive/Dropbox append the device name)
*-Mac Studio.*

# SQLite sidecar files (the runtime database lives in ~/.usmc, never in the repo)
*.db-wal
*.db-shm
*.sqlite-wal
*.sqlite-shm

# Multi-agent lock system (local only, never committed)
LOCK*.txt
